module:
  noProxy: none
  noVendor: ""
  private: '*.*'
  proxy: direct
  workspace: "off"

  hugoVersion:
    extended: false
    max: ""
    min: "0.56.0"
  mounts:
    - source: layouts
      target: layouts
